// Vanilla code for Creating a http server. Although we will use express framework for creating the
// server. But one should one know this.

// const http = require("http");
// // this server is also a event emitter so it has all the capabilities of event emitter
// const server = http.createServer((req, res) => {
//   if (req.url === "/") {
//     res.write("Hello World");
//     res.end();
//   }
//   if (req.url === "/api/courses") {
//     res.write(JSON.stringify([1, 2, 3]));

//     res.end();
//   }
// });

// server.listen(3000);
// console.log("Listening on port 3000...");
